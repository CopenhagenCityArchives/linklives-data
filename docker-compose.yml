version: '2'

services:
  ll-es:
    container_name: ll-es
    build:
      dockerfile: elasticsearch.Dockerfile
      context: .
    ports: 
      - "9200:9200"
      - "9300:9300"
    environment:
      discovery.type: single-node